apiVersion: v1
kind: ReplicationController
metadata:
  creationTimestamp: null
  labels:
    app: opssight
    component: artifactory-processor
    name: {{ .Release.Name }}
  name: {{ .Release.Name }}-opssight-artifactory-processor
  namespace: {{ .Release.Namespace }}
spec:
  {{- if eq .Values.status "Running" }}
    {{- if .Values.enableArtifactoryProcessor }}
  replicas: 1
    {{- else }}
  replicas: 0
    {{- end }}
  {{- else }}
  replicas: 0
  {{- end }}
  selector:
    app: opssight
    component: artifactory-processor
    name: {{ .Release.Name }}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: opssight
        component: artifactory-processor
        name: {{ .Release.Name }}
      name: {{ .Release.Name }}-opssight-artifactory-processor
    spec:
      containers:
      - args:
        - /etc/artifactory-processor/opssight.json
        command:
        - ./opssight-artifactory-processor
        envFrom:
        - secretRef:
            name: {{ .Release.Name }}-opssight-blackduck
        {{- if .Values.artifactoryProcessor.registry }}
          {{- if .Values.artifactoryProcessor.imageTag }}
        image: {{ .Values.artifactoryProcessor.registry }}/opssight-artifactory-processor:{{ .Values.artifactoryProcessor.imageTag }}
          {{- else }}
        image: {{ .Values.artifactoryProcessor.registry }}/opssight-artifactory-processor:{{ .Values.imageTag }}
          {{- end}}
        {{- else }}
          {{- if .Values.artifactoryProcessor.imageTag }}
        image: {{ .Values.registry }}/opssight-artifactory-processor:{{ .Values.artifactoryProcessor.imageTag }}
          {{- else }}
        image: {{ .Values.registry }}/opssight-artifactory-processor:{{ .Values.imageTag }}
          {{- end}}
        {{- end}}
        name: artifactory-processor
        ports:
        - containerPort: 3002
          protocol: TCP
        resources:
          requests:
            cpu: {{ .Values.artifactoryProcessor.requestCpu }}
            memory: {{ .Values.artifactoryProcessor.requestMemory }}
        volumeMounts:
        - mountPath: /etc/artifactory-processor
          name: artifactory-processor
        - mountPath: /tmp
          name: logs
      dnsPolicy: ClusterFirst
      {{- include "ops.imagePullSecrets" . | nindent 6 }}
      volumes:
      - configMap:
          defaultMode: 420
          name: {{ .Release.Name }}-opssight-opssight
        name: artifactory-processor
      - emptyDir: {}
        name: logs
status:
  replicas: 0
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: opssight
    component: artifactory-processor
    name: {{ .Release.Name }}
  name: {{ .Release.Name }}-opssight-artifactory-processor
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - name: port-artifactory-processor
    port: 3002
    protocol: TCP
    targetPort: 3002
  selector:
    app: opssight
    component: artifactory-processor
    name: {{ .Release.Name }}
  type: ClusterIP
status:
  loadBalancer: {}
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: {{ .Release.Name }}-opssight-artifactory-processor
  namespace: {{ .Release.Namespace }}
  labels:
    app: opssight
    component: artifactory-processor-exposed
    name: {{ .Release.Name }}
  annotations:
    openshift.io/host.generated: 'true'
spec:
  host: {{ .Release.Name }}-opssight-artifactory-processor-ops.apps.bizdev.synopsys-alliances.com
  subdomain: ''
  to:
    kind: Service
    name: {{ .Release.Name }}-opssight-artifactory-processor
    weight: 100
  port:
    targetPort: port-artifactory-processor
  wildcardPolicy: None
