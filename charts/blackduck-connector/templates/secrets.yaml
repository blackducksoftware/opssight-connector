apiVersion: v1
data:
  {{ .Values.blackduck.connectionsEnvironmentVariableName }}: {{ .Values.secret.externalHub | toJson | b64enc }}
  securedRegistries.json: {{ .Values.secret.securedRegistries | toJson | b64enc }}
kind: Secret
metadata:
  labels:
    app: opssight
    component: blackduck
    name: {{ .Release.Name }}
  name: {{ .Release.Name }}-opssight-blackduck
  namespace: {{ .Release.Namespace }}
type: Opaque
